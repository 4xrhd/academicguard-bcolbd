# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9            # File upload (multipart/form-data)

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0                    # Async PostgreSQL driver
psycopg2-binary>=2.9.9             # Sync driver (Alembic migrations)
alembic>=1.13.1

# ── Authentication / Security ─────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0   # JWT (HS256)
passlib[bcrypt]>=1.7.4             # bcrypt password hashing
bcrypt>=4.0.0,<4.1.0               # bcrypt backend (passlib incompatible with >=4.1)
pydantic[email]>=2.7.1             # Includes email-validator
pydantic-settings>=2.2.1
email-validator>=2.0.0             # Required by pydantic EmailStr
redis>=5.0.0                       # Redis async client for rate limiting

# pymupdf>=1.24.3                    # fitz — PDF text extraction (Removed)
-e ../../project/opendataloader-pdf/python/opendataloader-pdf

# ── NLP / AI Engine ───────────────────────────────────────────────────────────
sentence-transformers>=2.7.0       # all-MiniLM-L6-v2 semantic embeddings
transformers>=4.41.0               # GPT-2 for perplexity analysis
torch>=2.5.0                       # PyTorch (CPU; add +cu118 for GPU)
scikit-learn>=1.5.0                # TF-IDF, cosine similarity, logistic regression
nltk>=3.8.1                        # Tokenization, stopwords, lemmatization
numpy>=1.26.4
tree-sitter>=0.22.3                # AST generation for multi-language Code Forensics
tree-sitter-python>=0.21.0
tree-sitter-c>=0.21.4
tree-sitter-cpp>=0.22.2
tree-sitter-java>=0.21.0

# ── Reporting ─────────────────────────────────────────────────────────────────
reportlab>=4.2.0                   # PDF report generation
openpyxl>=3.1.2                    # Excel report generation
pandas>=2.2.2                      # Data manipulation for CSV/Excel

# ── HTTP client (GPTZero API + tests) ─────────────────────────────────────────
httpx>=0.27.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.6
